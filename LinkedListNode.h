#pragma once

class LinkedListNode
{
    LinkedListNode(int data, LinkedListNode *prev = nullptr, LinkedListNode *next = nullptr);
    
    int data;
    LinkedListNode* prev;
    LinkedListNode* next;
    /* data */
};
